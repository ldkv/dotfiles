# History searching
alias hg="history -i -D 0 | grep"

# Git aliases
alias gpt="git push --tags"

# Dotfiles aliases
alias dotfiles="cd $DOTFILES_PATH"
alias dotfiles-update="dotfiles && uvx copier update --trust -A && uv run -m scripts.update"
